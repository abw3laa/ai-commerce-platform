import {describe,it,expect} from "vitest";import Fastify from "fastify";import {adminPaymentsRoutes} from "../../src/routes/admin-payments.js";import {adminShippingRoutes} from "../../src/routes/admin-shipping.js";import {createFakeAuthRepositories} from "../helpers/fake-repositories.js";import {generateSessionToken,hashSessionToken} from "../../src/auth/session-token.js";import type {AuthorizationRepository} from "../../src/auth/authorization-types.js";import {createInMemoryPaymentRepository} from "../../src/payments/in-memory-payment-repository.js";import {createInMemoryShipmentRepository} from "../../src/shipping/in-memory-shipment-repository.js";
async function setup(perms:string[]){const app=Fastify(),auth=createFakeAuthRepositories([{id:"a1",email:"t@example.com",passwordHash:"x",isActive:true}]),token=generateSessionToken();await auth.adminSessionRepo.create({adminUserId:"a1",tokenHash:hashSessionToken(token),expiresAt:new Date(Date.now()+60000)});const authorizationRepo:AuthorizationRepository={async getPermissionKeysForAdmin(){return [...perms] as never;}};const payments=createInMemoryPaymentRepository(),shipping=createInMemoryShipmentRepository();await app.register(adminPaymentsRoutes,{deps:auth,authorizationRepo,paymentRepo:payments,isProduction:false});await app.register(adminShippingRoutes,{deps:auth,authorizationRepo,shipmentRepo:shipping,isProduction:false});return{app,token,payments,shipping};}
describe("admin payments and shipping",()=>{it("requires separate permissions",async()=>{const x=await setup([]);expect((await x.app.inject({method:"GET",url:"/orders/o1/payment",headers:{cookie:"admin_session="+x.token}})).statusCode).toBe(403);expect((await x.app.inject({method:"GET",url:"/orders/o1/shipping",headers:{cookie:"admin_session="+x.token}})).statusCode).toBe(403);await x.app.close();});it("manages payment verification",async()=>{const x=await setup(["payments"]);const c=await x.app.inject({method:"PUT",url:"/orders/o1/payment",headers:{cookie:"admin_session="+x.token},payload:{method:"bank_transfer",amount:3000,reference:"REF1"}});expect(c.statusCode).toBe(200);const v=await x.app.inject({method:"PATCH",url:"/payments/"+c.json().id+"/status",headers:{cookie:"admin_session="+x.token},payload:{status:"approved"}});expect(v.statusCode).toBe(200);await x.app.close();});it("manages shipment tracking",async()=>{const x=await setup(["shipping"]);const c=await x.app.inject({method:"PUT",url:"/orders/o1/shipping",headers:{cookie:"admin_session="+x.token},payload:{carrier:"Carrier",trackingCode:"TRK1",trackingUrl:"https://carrier.test/TRK1"}});expect(c.statusCode).toBe(200);const v=await x.app.inject({method:"PATCH",url:"/shipments/"+c.json().id+"/status",headers:{cookie:"admin_session="+x.token},payload:{status:"shipped"}});expect(v.statusCode).toBe(200);await x.app.close();});});