# AI Commerce Admin Android

Native Android admin client for the AI Commerce backend. It is not a WebView.

Set the backend URL on the login screen, then sign in with an existing AdminUser. The app stores only the session cookie and server URL locally. Minimum Android version: 8.0 (API 26).
