import type {FastifyInstance} from "fastify";import type {AuthRepositories} from "../auth/types.js";import type {AuthorizationRepository} from "../auth/authorization-types.js";import {createAuthGuard} from "../auth/auth-guard.js";import {createPermissionGuard} from "../auth/authorization-guard.js";import type {CommerceEngine} from "../ai/types.js";
export async function adminAiRoutes(app:FastifyInstance,o:{deps:AuthRepositories;authorizationRepo:AuthorizationRepository;engine:CommerceEngine;isProduction:boolean}){const auth=createAuthGuard({...o.deps,isProduction:o.isProduction}),guard=createPermissionGuard(o.authorizationRepo,"ai");app.post("/ai/respond",{preHandler:[auth,guard]},async(req,reply)=>{const b=req.body&&typeof req.body==="object"?req.body as Record<string,unknown>:{};if(typeof b.message!=="string"||!b.message.trim())return reply.code(400).send({error:"invalid_message"});try{return await o.engine.respond({message:b.message.trim(),customerId:typeof b.customerId==="string"?b.customerId:undefined,conversationId:typeof b.conversationId==="string"?b.conversationId:undefined,language:typeof b.language==="string"?b.language:undefined});}catch{return reply.code(502).send({error:"ai_provider_unavailable"});}});}